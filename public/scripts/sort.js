/*$(document).ready(function() {
    $('.sort-link').click(function () {
        var href = $(this).attr('href');
        if (href.endsWith('Asc')) {
            $(this).attr('href', href.replace('Asc', 'Desc'));
        } else if (href.endsWith('Desc')) {
            $(this).attr('href', href.replace('Desc', 'Asc'));
        }
    });
});*/